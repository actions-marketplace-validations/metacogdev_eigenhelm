# eh inspect

Inspect a saved model's metadata and statistics.

## Usage

```bash
eh inspect [OPTIONS] MODEL_PATH
```

## Example

```bash
eh inspect models/lang-python.npz
```

Displays: language, corpus class, principal component count, training vector count, calibration thresholds, and score distribution statistics.
